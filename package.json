{
  "name": "portfolio-v3",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix react run dev",
    "build": "npm --prefix react run build",
    "preview": "npm --prefix react run preview"
  }
}
